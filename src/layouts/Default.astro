---
import BaseNavigation from '@components/layout/BaseNavigation.astro';
import BaseFooter from '@components/layout/BaseFooter.astro';
import BaseHead from '../components/layout/BaseHead.astro';

interface Props {
  title: string;
  description: string;
  classList?: string;
  pageTitle?: string;
  ogImage?: URL;
  ogType?: string;
  pageSchema?: Record<string, any>;
  breadcrumbs?: Array<{name: string, url: string}>;
  customTitle?: string;
  isHomepage?: boolean;
  isBlogIndex?: boolean;
  blogPosts?: Array<{title: string, description: string, url: string, pubDate: string, author?: string}>;
  isTagPage?: boolean;
  tagName?: string;
  articleBody?: string;
  faqSchema?: Record<string, any> | null;
}

const { title, description, classList, ogImage, ogType, pageSchema, breadcrumbs, customTitle, isHomepage = false, isBlogIndex = false, blogPosts = [], isTagPage = false, tagName, articleBody, faqSchema } = Astro.props;
const pageTitle = Astro.props.pageTitle ?? `John Wheeler | ${title}`;
---

<html lang='en' class='h-full'>
  <BaseHead ogImage={ogImage} ogType={ogType} title={title} description={description} pageSchema={pageSchema} breadcrumbs={breadcrumbs} customTitle={customTitle} isHomepage={isHomepage} isBlogIndex={isBlogIndex} blogPosts={blogPosts} isTagPage={isTagPage} tagName={tagName} articleBody={articleBody} faqSchema={faqSchema}/>

  <body class:list={[classList]}>
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-5DFDJZF8"
      height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->
    <BaseNavigation {pageTitle} />
    <slot />
    <BaseFooter />
    
    <!-- Load global CSS after initial render -->
    <script is:inline>
      try {
        // Ensure global styles are loaded for non-critical elements
        if (!document.querySelector('link[href="/styles/global.css"]')) {
          const link = document.createElement('link');
          link.rel = 'stylesheet';
          link.href = '/styles/global.css';
          link.onerror = function() {
            console.warn('Failed to load global CSS');
          };
          document.head.appendChild(link);
        }
      } catch (error) {
        console.warn('Error loading global CSS:', error);
      }
    </script>
  </body>
</html>